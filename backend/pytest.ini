[pytest]
testpaths = tests
pythonpath = .
addopts = -v --tb=short
asyncio_mode = auto
markers =
    smoke: smoke tests - быстрая проверка основной функциональности каждого endpoint
    slow: медленные тесты
    integration: интеграционные тесты