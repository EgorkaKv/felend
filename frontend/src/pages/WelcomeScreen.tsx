import { Box, Typography } from '@mui/material';

const WelcomeScreen = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">Welcome Screen</Typography>
      <Typography>Онбординг - TODO</Typography>
    </Box>
  );
};

export default WelcomeScreen;
