import { Box, Typography } from '@mui/material';

const RegisterPage = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">Register Page</Typography>
      <Typography>Страница регистрации - TODO</Typography>
    </Box>
  );
};

export default RegisterPage;
