import { Box, Typography } from '@mui/material';

const LoginPage = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">Login Page</Typography>
      <Typography>Страница входа - TODO</Typography>
    </Box>
  );
};

export default LoginPage;
